DATABASE_URL="postgresql://postgres:postgres@localhost:5432/real_state_ai"
BETTER_AUTH_SECRET=46d95c0f6b3ccb5e124a9b24e88dd82cfdaf252f3a20c61c5182c7b74d892595
BETTER_AUTH_URL="http://localhost:3000"

# URL pública de la app (webhooks y enlaces). En local: http://localhost:3000
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Meta (WhatsApp Cloud API + Instagram messaging) — webhook único: GET/POST /api/webhooks/meta
META_WEBHOOK_VERIFY_TOKEN="definí_un_token_largo_y_coincidente_con_el_panel_de_meta"
META_APP_SECRET="app_secret_de_meta_para_validar_x_hub_signature_256"

# Cifrado de tokens de acceso (Graph API) en la base — 32 bytes en base64:
# openssl rand -base64 32
META_TOKEN_ENCRYPTION_KEY=""

# IA opcional para casos ambiguos (fallback híbrido sobre reglas):
# OPENAI_API_KEY=""
# OPENAI_MODEL="gpt-4.1-mini"

# Procesador interno de jobs críticos (/api/internal/jobs/process):
# INTERNAL_JOBS_SECRET=""

# Opcional: pruebas E2E (Playwright) — mismo usuario que creaste con create-account (agencia demo)
# E2E_USER_EMAIL="demo.operator@leadsignal.local"
# E2E_USER_PASSWORD=""
# PLAYWRIGHT_SKIP_WEBSERVER=1

# Crear operador desde terminal (sin registro web): npm run create-account
# CREATE_ACCOUNT_EMAIL=demo.operator@leadsignal.local
# CREATE_ACCOUNT_PASSWORD=""
# CREATE_ACCOUNT_NAME="Operador demo"
